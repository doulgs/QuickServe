import styled from "styled-components/native";

export const Container = styled.View`
  margin: ${(props) => props.theme.Size.lg} ${(props) => props.theme.Size.lg} 0
    ${(props) => props.theme.Size.lg};
`;
